# Native event journal

Version 1.1.0 adds a bounded in-memory event journal. Use the two native select entities to filter by camera and event type. The sensor exposes at most 25 compact entries and remains below Home Assistant Recorder's attribute-size limit.
