# Capability Explorer

The `Capability Explorer` sensor exposes a privacy-safe profile for every discovered Tapo camera.

It uses only Home Assistant device and entity registries. It performs no network polling and does not wake battery cameras.

Each camera profile includes observed model and firmware data, entity counts, source platforms, inferred capabilities, evidence levels, power-source hints, and a registry-evidence completeness score. Unknown data remains explicitly unknown.
