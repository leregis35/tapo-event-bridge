# Source Resolution Engine

Version 1.4 resolves normalized events to a readable camera name, model, source entity and transport. The last-event camera is now separate from the transport. Person lighting also supports Tapo floodlights exposed as sibling Home Assistant devices by matching camera and floodlight names conservatively.
