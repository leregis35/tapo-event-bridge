# Sprint 2 patch

Copy this archive into the repository root and replace existing files.

Run:

```powershell
uv run ruff check .
uv run pytest
```

Expected: Ruff passes and 6 tests pass.

Commit: `feat: add diagnostic evidence engine`
